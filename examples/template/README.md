## Template for working with the framework

To use this template, copy all the files to your workspace and add the library files. To build the project, use the command:

```console
$ cobc -x main.cbl index-page.cbl lib.o
```
